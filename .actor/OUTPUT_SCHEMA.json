{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of the ZORG-Ω Harvester",
    "properties": {
        "results": {
            "type": "string",
            "title": "Final Exhibitor Dataset",
            "description": "The complete collection of extracted exhibitor data stored in the default dataset.",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}